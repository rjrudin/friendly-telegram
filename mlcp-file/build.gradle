plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

dependencies {
    api project(":mlcp-core")

    testImplementation(testFixtures(project(":mlcp-core")))
}

test {
    useJUnitPlatform()
}
