plugins {
    id 'java-library'
}

repositories {
    jcenter()
    mavenCentral()
}

dependencies {
    api project(":mlcp-core")

    testImplementation(testFixtures(project(":mlcp-core")))
}

test {
    useJUnitPlatform()
}
